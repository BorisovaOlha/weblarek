# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные

#### Интерфейс IProduct
Используется для учета товаров, отображаемых в приложении. Включает в себя id товара, название товара, описание товара, изображение товара, цену товара (число или null) и категорию, к которой принадлежит товар.

interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}

#### Интерфейс IBuyer
Используется для учета данных, указанных покупателем при оформлении покупки. Включает в себя выбранный способ оплаты (карта, наличные, выбор не сделан), адрес, почтовый адрес и номер телефона.

interface IBuyer {
  payment: "card" | "cash" | "";
  email: string;
  phone: string;
  address: string;
}

### Модели данных

#### Класс Products
Реализует интерфейс `IProduct`.
Класс отвечает за хранение массива всех товаров, которые можно купить в приложении, а также за хранение товара, выбранного для подробного отображения.

Поля класса:  
`items: IProduct[]` - поле для хранения массива всех товаров.
`selectedItem: IProduct` - поле для хранения товара, выбранного для подробного отображения.

Методы класса:
`setItems(items: IProduct[]): void` - принимает в параметрах массив товаров, сохраняет его.
`getItems(): IProduct[]` - возвращает массив товаров.
`setItem(selectedItem: IProduct): void` - сохраняет товар, выбранный для подробного отображения.
`getItem(): IProduct` - возвращает товар, выбранный для подробного отображения.
`getItemById(id: string): IProduct | undefined` - принимает в параметрах id товара и возвращает соответствующий товар.

#### Класс Basket
Реализует интерфейс `IProduct`.
Класс отвечает за хранение массива товаров, выбранных покупателем для покупки.

Поля класса:
`basketItems: IProduct[]` - поле для хранения массива товаров, выбранных покупателем для покупки.

Методы класса:
`getBasketItems(): IProduct[]` - возвращает массив товаров, которые находятся в корзине.
`addItem(item: IProduct): void` - добавляет в массив корзины товар, переданный в параметрах.
`removeItem(item: IProduct): void` - удаляет из корзины товар, переданный в параметрах.
`emptyBasket(): void` - очищает корзину.
`getTotalPrice(): number` - возвращает стоимость всех товаров в корзине.
`getItemsTotal(): number` - возвращает количество товаров в корзине.
`checkItemById(id: string): boolean` - проверяет наличие товара в корзине по id, переданному в параметрах.


#### Класс Buyer
Класс реализует интерфейс `IBuyer` и отвечает за данные, относящиеся к покупателю.

Поля класса:
`payment: "card" | "cash" | ""`
`email: string`
`phone: string`
`address: string`

Методы класса:
`setData(key: string, value: string): void` - сохраняет по отдельности данные покупателя, переданные в параметрах.
`getData(): IBuyer` - возвращает все данные покупателя.
`emptyData(): void` - очищает данные покупателя.
`checkData(): Partial<IBuyer>` - проверяет данные покупателя и возвращает результат проверки в виде объекта с полями, которые не были заполнены покупателем.


### Слой коммуникации

#### Класс WebLarekApi
Отвечает за получение данных с сервера и отправку данных на сервер. Использует функциональность класса, переданного в конструктор и реализующего интерфейс `IApi`. Методы класса возвращают промисы с типами `IProduct[]`, `IOrderResponse` и `IErrorResponse`.

Конструктор:  
`constructor(localApi: IApi)` - в конструктор в качестве параметра передается класс, реализующий интерфейс `IApi`.

Поля класса:  
`localApi: IApi` - данные и методы для запроса, взятые из класса, переданного в качестве параметра.

Методы:  
`getData(): Promise<IProduct[]>` - выполняет GET запрос на ендпоинт `/product/` и возвращает промис с объектом, которым ответил сервер.  
`postData(order: IOrderRequest): Promise<IOrderResponse | IErrorResponse>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт `/order/`.

### Слой представления

#### Класс Header
Класс является наследником компонента `Component`. Реализует интерфейс `IHeaderData` и отвечает за отображение элементов, находящихся внутри шапки (`.header`).

Конструктор:  
`constructor(protected events: IEvents, container: HTMLElement)` - принимает брокер событий и ссылку на корневой DOM элемент, за отображение которого он отвечает.

Поля класса:  
`basketButton: HTMLButtonElement` - хранит найденный элемент иконки корзины.
`counterElement: HTMLElement` - хранит найденный элемент счетчика корзины.

Методы:  
`set counter(value: number)` - устанавливает новое значение счётчика в разметке.

#### Класс Gallery
Класс является наследником компонента `Component`. Реализует интерфейс `IGalleryData` и отвечает за отображение элементов, находящихся внутри блока разметки `.gallery`.

Конструктор:
`constructor(catalogElement: HTMLElement)` - принимает ссылку на элемент DOM, в который выводятся карточки товаров.

Поля класса:  
`catalogElement: HTMLElement` - хранит элемент DOM, в который выводятся карточки товаров.

Методы:  
`set catalog(items: HTMLElement[])` - вставляет в разметку карточки товаров, переданные в параметрах.


#### Класс Modal
Класс является наследником компонента `Component`. Реализует интерфейс `IModalData` и отвечает за отображение модального окна (блок разметки `.modal`).

Конструктор:
`constructor(protected events: IEvents, container: HTMLElement)` - принимает брокер событий и элемент DOM, за отображение которого отвечает.

Поля класса:  
`contentContainer: HTMLElement` - хранит элемент DOM, в который выводится содержимое модального окна.
`closeButton: HTMLButtonElement` - хранит элемент кнопки, которая закрывает модальное окно.

Методы:
`showModal(isVisible: boolean)` - переключает видимость модального окна.
`set content(modalContent: HTMLElement)` - принимает контент, который будет отображаться в модальном окне.

#### Класс OrderSuccess
Класс является наследником компонента `Component`. Реализует интерфейс `IOrderSuccess` и отвечает за отображение темплейта `#success`, который сообщает об успешном оформлении заказа.

Конструктор:
`constructor(protected events: IEvents, container: HTMLElement)` - принимает брокер событий и ссылку на DOM элемент, за отображение которого отвечает.

Поля класса:
`continueButton: HTMLButtonElement` - хранит элемент кнопки, которая возвращает к покупкам.
`purchaseSum: HTMLElement` - хранит элемент DOM, в который выводится оплаченная сумма.

Методы:
`set sum(value: number)` - принимает в параметрах оплаченную сумму и подставляет ее в соответствующий элемент DOM.

#### Класс Card
Класс является наследником класса `Component`. Отвечает за отображение карточки товара и объединяет в себе функционал, общий для дочерних классов.

Конструктор:
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент, за отображение которого отвечает.

Поля класса:
`titleElement: HTMLElement` - хранит элемент DOM с названием товара.
`priceElement: HTMLElement` - хранит элемент DOM с ценой товара.

Методы:  
`set title(value: string)` - принимает в параметрах название товара и подставляет его в соответствующий элемент DOM.
`set price(value: number | null)` - принимает в параметрах цену товара и подставляет ее в соответствующий элемент DOM.

#### Класс СardСatalog
Класс является наследником класса `Card`. Отвечает за отображение карточки товара с темплейтом `#card-catalog`, которая предназначена для подстановки в основной каталог.

Конструктор:
`constructor(container: HTMLElement, actions?: ICardActions)` - принимает обработчик событий и ссылку на DOM элемент, за отображение которого отвечает.

Поля класса:
`categoryElement: HTMLElement` - хранит элемент DOM с категорией товара.
`imageElement: HTMLImageElement` - хранит элемент DOM с изображением товара.

Методы:  
`set category(value: string)` - принимает категорию товара в виде строки и подставляет его в соответствующий элемент DOM.
`set image(value: string)` - принимает ссылку на изображение товара и использует метод родителя для подстановки изображения в соответствующий элемент DOM.

#### Класс CardPreview
Класс является наследником класса `Card`. Отвечает за отображение карточки товара с темплейтом `#card-preview`, которая предназначена для показа полной информации о товаре в модальном окне.

Конструктор:
`constructor(protected events: IEvents, container: HTMLElement)` - принимает брокер событий и ссылку на DOM элемент, за отображение которого отвечает.

Поля класса:
`categoryElement: HTMLElement` - хранит элемент DOM с категорией товара.
`imageElement: HTMLImageElement` - хранит элемент DOM с изображением товара.
`textElement: HTMLElement` - хранит элемент DOM с описанием товара.
`cardButton: HTMLButtonElement` - хранит элемент DOM с кнопкой, которая добавляет товар в корзину или удаляет товар из корзины.

Методы:  
`set category(value: string)` - принимает категорию товара в виде строки и подставляет его в соответствующий элемент DOM.
`set image(value: string)` - принимает ссылку на изображение товара и использует метод родителя для подстановки изображения в соответствующий элемент DOM.
`set description(value: string)` - принимает описание товара в виде строки и подставляет его в соответствующий элемент DOM.
`changeButton(inBasket: boolean)` - меняет текстовое содержимое кнопки с 'В корзину' на 'Удалить из корзины' и обратно.
`disableButton(isDisabled: boolean)` - блокирует кнопку 'В корзину' и меняет ее текст на 'Недоступно'.

#### Класс CardBasket
Класс является наследником класса `Card`. Отвечает за отображение карточки товара с темплейтом `#card-basket`, которая предназначена для показа в корзине покупок.

Конструктор:
`constructor(container: HTMLElement, actions?: ICardActions)` - принимает обработчик событий и ссылку на DOM элемент, за отображение которого отвечает.

Поля класса:
`indexElement: HTMLElement` - хранит элемент DOM, в котором отображается порядковый номер товара в корзине.
`deleteButton: HTMLButtonElement` - хранит элемент DOM с кнопкой удаления товара из корзины.

Методы:
`set index(value: number)` - принимает порядковый номер товара в корзине для его отображения в соответствующем элементе DOM.

#### Класс BasketView
Класс является наследником компонента `Component`. Реализует интерфейс `IBasketView` и отвечает за отображение корзины покупок (темплейт `#basket`).

Конструктор:
`constructor(protected events: IEvents, container: HTMLElement)` - принимает брокер событий и элемент DOM, за отображение которого отвечает.

Поля класса:  
`basketButton: HTMLButtonElement` - хранит элемент кнопки, которая запускает оформление заказа.
`basketPriceElement: HTMLElement` - хранит элемент DOM, в который выводится общая стоимость товаров в корзине.
`contentContainer: HTMLElement` - хранит элемент DOM, в который выводится список оформляемых товаров.

Методы:
`set price(value: number | null)` - принимает общую стоимость товаров в корзине и подставляет ее в соответствующий элемент DOM.
`set content(items: HTMLElement[])` - принимает список товаров и вставляет его в разметку корзины.
`set buttonDisabled(isDisabled: boolean)` - блокирует кнопку оформления заказа.

#### Класс Form
Класс является наследником класса `Component`. Отвечает за отображение формы оформления заказа и объединяет в себе функционал, общий для дочерних классов.

Конструктор:
`constructor(protected events: IEvents, container: HTMLElement)` - принимает брокер событий и ссылку на DOM элемент, за отображение которого отвечает.

Поля класса:
`inputElements: HTMLInputElement[]` - хранит элементы полей, в которые покупатель вводит свои данные.
`submitButton: HTMLButtonElement` - хранит элемент кнопки с типом "sumbit".
`errorField: HTMLElement` - хранит элемент DOM, в который выводится сообщение об ошибке заполнения формы.

Методы:  
`set error(errorMessage: string)` - принимает в параметрах сообщение об ошибке и подставляет его в соответствующий элемент DOM.
`set ready(isReady: boolean)` - блокирует кнопку отправки данных из формы.

#### Класс OrderForm
Класс является наследником класса `Form`. Отвечает за отображение формы с темплейтом `#order`, где пользователь выбирает способ оплаты и заполняет адрес.

Конструктор:
`constructor(protected events: IEvents, container: HTMLElement)` - принимает брокер событий и ссылку на DOM элемент, за отображение которого отвечает.

Поля класса:
`paymentButtons: HTMLButtonElement[]` - хранит элементы кнопок, с помощью которых пользователь выбирает способ оплаты.
`addressInput: HTMLInputElement` - хранит элемент поля, в котором покупатель вводит свой адрес.

Методы:
`set payment(value: "" | "card" | "cash")` - принимает способ оплаты и выделяет соответствующую кнопку в интерфейсе.

#### Класс ContactsForm
Класс является наследником класса `Form`. Отвечает за отображение формы с темплейтом `#contacts`, где пользователь вводит email и номер телефона и завершает оформление покупки.

Конструктор:
`constructor(protected events: IEvents, container: HTMLElement)` - принимает брокер событий и ссылку на DOM элемент, за отображение которого отвечает.

Поля класса:
`emailInput: HTMLInputElement` -  хранит элемент поля, в котором покупатель вводит свой email.
`phoneInput: HTMLInputElement` - хранит элемент поля, в котором покупатель вводит свой телефон.

### Презентер
Код презентера представлен в основном скрипте приложения, а именно `main.ts`. Презентер отвечает за связывание всех слоёв приложения: моделей данных и компонентов представления (View). Он обрабатывает события, сгенерированные пользователем или моделями, и обновляет соответствующие компоненты интерфейса.


### Генерация событий
#### События слоя представления
`product:selected` - выбор пользователем карточки для подробного отображения (класс `CardCatalog`).

`cardButton:click` - нажатие на кнопку "В корзину" (`.card__button`, она же "Удалить из корзины") в превью карточки товара (`#card-preview`). Событие генерируется в классе `CardPreview`.

`basketItem:deleted` - нажатие на кнопку удаления товара (`.basket__item-delete`, класс `СardBasket`) в корзине покупок.

`basket:open` - нажатие на иконку корзины(`.header__basket`) в шапке (`header`). Действие открывает корзину с товарами. Событие генерируется в классе `Header`.

`basketButton:clicked` - нажатие на кнопку "Оформить" (`.basket__button`) в корзине (`#basket`). Событие генерируется в классе `BasketView`.

`payment:select`- выбор пользователем способа оплаты в классе `OrderForm`.

`address:input` - заполнение пользователем поля с адресом в классе `OrderForm`.

`order:continue` - нажатие на кнопку "Далее" в классе `OrderForm`.

`email:input` - заполнение пользователем поля с электронной почтой в классе `ContactsForm`.

`phone:input` - заполнение пользователем поля с номером телефона в классе `ContactsForm`.

`order:pay` - нажатие на кнопку оплатить в классе `ContactsForm`.

`modal:closed` - нажатие на кнопку закрытия модального окна (`.modal__close`) или на фон в блоке разметки с классом `.modal`. Событие генерируется в классе `Modal`.

`shopping:continue` - нажатие на кнопку возврата к покупкам в классе `OrderSuccess`.


#### События моделей данных
`catalog:changed` - изменение каталога товаров. Событие генерируется в классе `Products.ts`.

`selectedProduct:changed` - изменение продукта, выбранного для подробного отображения. Событие генерируется в классе `Products.ts`.

`buyerData:changed` - изменение данных покупателя. Событие генерируется в классе `Buyer.ts`.

`basket:changed` - изменения в корзине с товарами. Событие генерируется в классе `Basket.ts`.





